## To run the Pi server
1. Go to Pi directory and install dependencies
  ```bash
   npm install
   ```
2. Start redis 
  ```bash
  redis-server
  ```
3. After redis is connected, start Pi server
  ```bash
  node index.js
  ```
